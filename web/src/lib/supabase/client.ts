// Supabase Browser Client — Stub for Session D
//
// TODO (Session D): Install @supabase/supabase-js and @supabase/ssr
// TODO (Session D): Read NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY from env
// TODO (Session D): Create actual browser client using createBrowserClient()

/**
 * Get Supabase client for use in Client Components.
 * Returns null when auth is not yet connected.
 */
export function getSupabaseClient() {
  // TODO (Session D): Wire to Supabase
  // return createBrowserClient(
  //   process.env.NEXT_PUBLIC_SUPABASE_URL!,
  //   process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  // )
  return null;
}
