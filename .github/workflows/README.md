# PPL± GitHub Actions

Planned Workflows for the PPL± repository CI pipeline.

---

## Planned Workflows

**`lint.yml`** (not yet written)
Three-tier validation on push/PR against `cards/`.
See `.github/linters/README.md` for tier details.

---

## Status: PLANNED

No CI workflows are active yet beyond the existing `pylint.yml`.
The linting pipeline will be built when whiteboard.md promotes it
to active work. See `seeds/linters-architecture.md` for the plan.

🧮
