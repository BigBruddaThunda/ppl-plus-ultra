/**
 * canvas/src/rendering/index.ts — Rendering Pipeline Public API
 *
 * Re-exports all rendering module exports.
 * Implementations are added in the GREEN phase (TDD).
 */

// Barrel — populated after RED phase confirms test failures
