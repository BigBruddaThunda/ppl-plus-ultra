# Intake

Raw ideas, fragments, connections, and fractal bursts land here.

**Format:** `YYYY-MM-DD-[slug].md`
**Tagging:** Minimal SCL context. Relevant project (if obvious), relevant Order (if clear), relevant emojis.
**Rules:** No formatting requirements. No filtering. Capture everything.

Intake files stay here until a Ralph Loop session sorts them into project directories.
Do not move files from intake without a deliberate Ralph Loop.

The intake directory is a landing zone, not a trash can. Everything here has value. It just hasn't been assigned a room yet.
