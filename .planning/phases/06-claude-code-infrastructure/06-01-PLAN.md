---
phase: 06-claude-code-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/blank-canvas/SKILL.md
  - .claude/agents/canvas-renderer.md
  - canvas/components/.gitkeep
  - .claude/settings.json
  - .claude/AGENT-BOUNDARIES.md
autonomous: true
requirements:
  - CLCD-01
  - CLCD-02
  - CLCD-03

must_haves:
  truths:
    - "/blank-canvas skill exists and loads canvas workspace state when invoked"
    - "canvas-renderer subagent is defined with writes scoped to canvas/components/ only"
    - "PostToolUse hook fires on canvas/ file writes and never exits non-zero"
    - "AGENT-BOUNDARIES.md documents canvas-renderer scope and path-gating rules"
  artifacts:
    - path: ".claude/skills/blank-canvas/SKILL.md"
      provides: "Canvas workspace initialization skill"
      contains: "allowed-tools: Read, Bash, Grep, Glob"
    - path: ".claude/agents/canvas-renderer.md"
      provides: "Canvas component rendering subagent"
      contains: "canvas/components/"
    - path: "canvas/components/.gitkeep"
      provides: "Write target directory for canvas-renderer"
    - path: ".claude/settings.json"
      provides: "PostToolUse hook entry for canvas/"
      contains: "canvas/"
    - path: ".claude/AGENT-BOUNDARIES.md"
      provides: "Governance documentation for canvas-renderer"
      contains: "canvas-renderer"
  key_links:
    - from: ".claude/settings.json"
      to: "canvas/"
      via: "PostToolUse hook path gate"
      pattern: "grep -q 'canvas/'"
    - from: ".claude/agents/canvas-renderer.md"
      to: "canvas/components/"
      via: "scope constraint in agent body"
      pattern: "canvas/components/"
    - from: ".claude/AGENT-BOUNDARIES.md"
      to: ".claude/agents/canvas-renderer.md"
      via: "agent roster and per-agent section"
      pattern: "canvas-renderer"
---

<objective>
Wire Claude Code infrastructure for the canvas/ workspace: a /blank-canvas initialization skill, a canvas-renderer subagent scoped to canvas/components/, and a PostToolUse hook that fires on canvas/ file writes. Update AGENT-BOUNDARIES.md to document the new agent and canvas hook governance.

Purpose: Make the canvas/ workspace a first-class Claude Code citizen with proper agent boundaries, skill invocation, and write-time logging — completing the infrastructure layer before scripts and templates in Phases 7-8.

Output: 5 files created or modified; canvas/ workspace has skill + subagent + hook + governance documentation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-claude-code-infrastructure/06-RESEARCH.md

<interfaces>
<!-- Existing project patterns the executor needs. -->

From .claude/skills/generate-card/SKILL.md (frontmatter template):
```yaml
---
name: generate-card
description: Generate a single Ppl± workout card from its zip code. Reads identity doc, validates, generates, renames stub, logs to whiteboard.
disable-model-invocation: true
argument-hint: "[zip-code e.g. ⛽🌹🛒🔵]"
allowed-tools: Read, Write, Edit, Bash, Grep, Glob
---
```

From .claude/agents/card-generator.md (write-permitted agent template):
```yaml
---
name: card-generator
description: Generates a single Ppl± workout card in isolated context. Use when generating cards to keep main conversation clean.
tools: Read, Write, Edit, Bash, Grep, Glob
model: claude-opus-4-6
skills:
  - generate-card
---
```

From .claude/settings.json (existing PostToolUse hook — the exact pattern to follow):
```json
{
  "matcher": "Edit|Write",
  "hooks": [
    {
      "type": "command",
      "command": "FILE=$(echo \"$CLAUDE_TOOL_INPUT\" | python3 -c \"import sys, json; d=json.load(sys.stdin); print(d.get('file_path') or d.get('path') or '')\" 2>/dev/null); if echo \"$FILE\" | grep -q 'cards/'; then echo \"--- Auto-validating: $FILE ---\"; python scripts/validate-card.py \"$FILE\" 2>&1 || true; fi"
    }
  ]
}
```

From .claude/AGENT-BOUNDARIES.md (existing agent roster table format — line 12):
```markdown
| Agent | Type | Model | Role |
|-------|------|-------|------|
| **Claude Code** | Primary agent | claude-sonnet-4-6 | Session orchestration, architecture decisions, file writes across repo |
| **card-generator** | Subagent | claude-opus-4-6 | Single-card generation in isolated context |
| **deck-auditor** | Subagent | claude-sonnet-4-6 | Read-only SCL compliance audit of completed decks |
| **progress-tracker** | Subagent | claude-haiku-4-5 | Lightweight repo state scan and progress reporting |
```

From .claude/AGENT-BOUNDARIES.md (existing R/W matrix — line 24, canvas row):
```markdown
| `canvas/**` | R/W | R/W | — | — | — |
```

From .claude/AGENT-BOUNDARIES.md (Canvas Path-Gating section — lines 150-173):
Section exists with path-gate pattern, rules, and dual hierarchy note. Phase 6 UPDATES this section (does not create a new one).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create blank-canvas skill, canvas-renderer agent, and components directory</name>
  <files>.claude/skills/blank-canvas/SKILL.md, .claude/agents/canvas-renderer.md, canvas/components/.gitkeep</files>
  <action>
Create three files using existing project patterns as exact templates.

**1. `.claude/skills/blank-canvas/SKILL.md`** (CLCD-01)

Frontmatter:
- name: blank-canvas
- description: "Initialize the canvas workspace and load current state. Reads canvas/src/ structure, reports phase completion, confirms agent is scoped to canvas/ paths only."
- disable-model-invocation: true
- allowed-tools: Read, Bash, Grep, Glob (NO Write, NO Edit — this is a read-only orientation skill)

Body — 4-step workflow:
1. Load Canvas Workspace State: Read canvas/package.json, list canvas/src/ modules, list canvas/tests/
2. Report Phase Completion: Check existence of Phase 1-5 artifacts (canvas/src/types/scl.ts, canvas/src/weights/, canvas/src/tokens/, canvas/src/rendering/)
3. Load Boundary Rules: Read .claude/AGENT-BOUNDARIES.md canvas section. Confirm writes only to canvas/ paths. No writes to cards/, html/, web/, middle-math/.
4. Report Current State: Output summary of which phases are complete, which tests pass, what canvas/components/ contains.

Do NOT include an argument-hint field — the skill takes no arguments (optional phase focus comes from $ARGUMENTS naturally).

**2. `.claude/agents/canvas-renderer.md`** (CLCD-02)

Frontmatter:
- name: canvas-renderer
- description: "Generates canvas/components/ HTML or TSX rendering artifacts. Scoped strictly to canvas/components/ — never writes to cards/, html/, or web/."
- tools: Read, Write, Edit, Bash, Grep, Glob
- model: claude-sonnet-4-6
- No disallowedTools (it needs Write/Edit for canvas/components/)
- No skills field (no existing skill covers this workflow)

Body — SCOPE CONSTRAINT FIRST (before any workflow steps), then workflow:
- Scope constraint: "Before any write: 1. Confirm destination starts with canvas/components/. 2. If not — STOP. Report the conflict. Do not write."
- What it may write: canvas/components/** (HTML, TSX, CSS files only)
- What it never touches: cards/**, html/**, web/**, middle-math/**
- Before generating: Read canvas/src/rendering/index.ts (public rendering API), Read canvas/src/tokens/tokens.ts (token structure), Confirm CSS custom properties exist in weightsToCSSVars() output
- Art direction constraint: intaglio/banknote engraving aesthetic — fine hatching patterns for backgrounds (not solid fills), high-contrast linework (not soft gradients), guilloche-style borders, serif or mono typography (never rounded sans). Apply via CSS classes, never inline styles.

**3. `canvas/components/.gitkeep`** — empty file so the directory exists for the subagent's write target.
  </action>
  <verify>
    <automated>test -f .claude/skills/blank-canvas/SKILL.md && grep -q "allowed-tools: Read, Bash, Grep, Glob" .claude/skills/blank-canvas/SKILL.md && echo "SKILL OK" && test -f .claude/agents/canvas-renderer.md && grep -q "canvas/components/" .claude/agents/canvas-renderer.md && echo "AGENT OK" && test -f canvas/components/.gitkeep && echo "GITKEEP OK" && cd canvas && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>blank-canvas SKILL.md exists with read-only allowed-tools (no Write/Edit). canvas-renderer.md exists with scope constraint as first instruction and canvas/components/ as the only write target. canvas/components/.gitkeep exists. Existing canvas tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PostToolUse hook and update AGENT-BOUNDARIES.md</name>
  <files>.claude/settings.json, .claude/AGENT-BOUNDARIES.md</files>
  <action>
**1. `.claude/settings.json`** — Add a new PostToolUse entry (CLCD-03)

Add a NEW entry to the PostToolUse array (do NOT merge with the existing cards/ hook). The new entry goes AFTER the existing cards/ hook entry (index 0) and BEFORE the gsd-context-monitor entry (index 1). It must have:

- "matcher": "Edit|Write" (same as cards/ hook)
- Hook command pattern (mirrors existing cards/ hook exactly):
  ```
  FILE=$(echo "$CLAUDE_TOOL_INPUT" | python3 -c "import sys, json; d=json.load(sys.stdin); print(d.get('file_path') or d.get('path') or '')" 2>/dev/null); if echo "$FILE" | grep -q 'canvas/'; then echo "--- Canvas write: $FILE ---"; if echo "$FILE" | grep -qv 'canvas/components/\|canvas/tests/\|canvas/src/'; then echo "INFO: Write to canvas/ root path: $FILE"; fi; echo "Art direction: intaglio aesthetic — see AGENT-BOUNDARIES.md canvas section"; fi
  ```

CRITICAL: No `|| true` needed at the end because the `if` structure already handles the case where grep finds no match (it just doesn't enter the block). But DO ensure there is no path where the command exits non-zero. The `2>/dev/null` on python handles python failures. The if/fi handles grep. This matches the existing cards/ hook pattern.

Do NOT modify the existing cards/ hook entry or the gsd-context-monitor entry. Append only.

**2. `.claude/AGENT-BOUNDARIES.md`** — Update existing sections (CLCD-03 governance)

Three targeted updates to the EXISTING document (no new top-level sections):

a) **Agent Roster table** (around line 12): Add a new row:
   `| **canvas-renderer** | Subagent | claude-sonnet-4-6 | Canvas component generation scoped to canvas/components/ only |`

b) **Read/Write Matrix** (around line 24): Update the `canvas/**` row to add the canvas-renderer column. The matrix currently has 6 columns (Claude Code, Codex, card-generator, deck-auditor, progress-tracker). Add canvas-renderer as column 7. The canvas-renderer row value for `canvas/**` should show "R/W (components/ only)". For the new `canvas/components/**` path, add a dedicated row: `| canvas/components/** | R/W | R/W | — | — | — | R/W |`

c) **Per-Agent Write Constraints section** (after the progress-tracker subsection, around line 88): Add a new subsection:
   ```
   ### canvas-renderer Subagent
   - Writes: **only to canvas/components/**. Any other write is a boundary violation.
   - Reads: canvas/src/ (rendering API), canvas/src/tokens/ (token structure), .claude/AGENT-BOUNDARIES.md
   - Never writes to: cards/, html/, web/, middle-math/
   - Art direction: intaglio/banknote engraving aesthetic enforced on all component output
   - "Flush before delegate" rule: if this agent produces stdout output before delegating to another tool, stdout must be flushed first to avoid interleaved output
   ```

d) **Canvas Path-Gating section** (lines 150-173): Update to note that the PostToolUse hook is now LIVE (not just documented as a pattern). Add after the existing rules:
   ```
   - PostToolUse hook is LIVE in .claude/settings.json — logs all canvas/ writes and prints art direction reminder
   - canvas-renderer subagent is defined at .claude/agents/canvas-renderer.md — scope: canvas/components/ only
   ```
  </action>
  <verify>
    <automated>grep -q "canvas/" .claude/settings.json && echo "HOOK OK" && grep -q "canvas-renderer" .claude/AGENT-BOUNDARIES.md && echo "BOUNDARIES OK" && node -e "const s=require('./.claude/settings.json'); const hooks=s.hooks.PostToolUse; console.log('PostToolUse entries:', hooks.length); const canvasHook=hooks.find(h=>h.hooks && h.hooks[0] && h.hooks[0].command && h.hooks[0].command.includes('canvas/')); console.log('Canvas hook found:', !!canvasHook)" && cd canvas && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>settings.json has a new PostToolUse entry for canvas/ writes (separate from cards/ hook). AGENT-BOUNDARIES.md has canvas-renderer in agent roster, R/W matrix, per-agent constraints, and updated Canvas Path-Gating section. Existing canvas tests still pass. Total PostToolUse entries is 3 (cards hook, canvas hook, gsd-context-monitor).</done>
</task>

</tasks>

<verification>
Phase 6 deliverables are configuration files, not code. Verification is by file inspection plus regression:

1. **File existence:** All 5 files exist at correct paths
2. **Content correctness:** grep confirms key content strings in each file
3. **Regression:** `cd canvas && npm test` passes (Phase 1-5 tests unbroken)
4. **settings.json valid JSON:** `node -e "require('./.claude/settings.json')"` succeeds
5. **No exit code 2 in hook:** grep confirms hook command contains no `exit 2` or `process.exit(2)`
6. **Skill is read-only:** grep confirms blank-canvas SKILL.md does NOT contain Write or Edit in allowed-tools
</verification>

<success_criteria>
1. `/blank-canvas` skill exists with read-only tools (Read, Bash, Grep, Glob) and 4-step workspace initialization workflow
2. `canvas-renderer` agent exists with scope constraint as FIRST instruction, limited to canvas/components/ writes
3. PostToolUse hook in settings.json fires on canvas/ writes, logs the path, and prints art direction reminder — never exits non-zero
4. AGENT-BOUNDARIES.md documents canvas-renderer in all 4 relevant sections (roster, matrix, per-agent constraints, path-gating)
5. canvas/components/.gitkeep exists as write target for the subagent
6. Existing canvas/ test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/06-claude-code-infrastructure/06-01-SUMMARY.md`
</output>
