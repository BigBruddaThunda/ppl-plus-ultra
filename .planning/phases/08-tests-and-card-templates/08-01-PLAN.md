---
phase: 08-tests-and-card-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - canvas/tests/weight-vectors.test.ts
  - canvas/tests/keyword-routing.test.ts
  - canvas/tests/pipeline-integration.test.ts
autonomous: true
requirements:
  - TEST-02
  - TEST-03
  - TEST-05

must_haves:
  truths:
    - "10 representative zips produce expected CSS property values when resolved through the full weight pipeline"
    - "Known fitness terms route to correct dimension positions via scoreText()"
    - "Text input chains through scoreText() -> weightsToCSSVars() without error, producing 30+ CSS vars"
  artifacts:
    - path: "canvas/tests/weight-vectors.test.ts"
      provides: "TEST-02: 10 zip spot-check tests"
      contains: "resolveZip"
    - path: "canvas/tests/keyword-routing.test.ts"
      provides: "TEST-03: keyword dimension routing tests"
      contains: "scoreText"
    - path: "canvas/tests/pipeline-integration.test.ts"
      provides: "TEST-05: full pipeline integration tests"
      contains: "weightsToCSSVars"
  key_links:
    - from: "canvas/tests/weight-vectors.test.ts"
      to: "canvas/src/weights/resolver.ts"
      via: "resolveZip() import"
      pattern: "resolveZip\\("
    - from: "canvas/tests/pipeline-integration.test.ts"
      to: "canvas/src/parser/scorer.ts"
      via: "scoreText() -> weightsToCSSVars() chain"
      pattern: "scoreText.*weightsToCSSVars"
---

<objective>
Validate the full Ppl± pipeline with a comprehensive Vitest suite: weight vector spot-checks across 10 representative zips (all 7 Orders), keyword scoring dimension routing, and a full end-to-end integration test from text input to CSS custom properties.

Purpose: This is the capstone validation — proving the entire pipeline (Phases 1-5) works correctly for representative inputs before building the visual card layer.
Output: 3 new test files added to the existing 8-file Vitest suite.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@canvas/tests/rendering.test.ts
@canvas/src/weights/resolver.ts
@canvas/src/parser/scorer.ts
@canvas/src/rendering/weights-to-css-vars.ts
@canvas/src/rendering/saturation-map.ts
@canvas/src/tokens/tokens.ts
@canvas/src/parser/parse-result.ts
@canvas/src/types/scl.ts

<interfaces>
<!-- Key imports the executor needs -->

From canvas/src/weights/resolver.ts:
```typescript
export function resolveZip(order: number, axis: number, type: number, color: number): Float32Array;
```

From canvas/src/parser/scorer.ts:
```typescript
export function scoreText(input: string, maxCandidates?: number): ParseResult[];
```

From canvas/src/rendering/weights-to-css-vars.ts:
```typescript
export function weightsToCSSVars(vector: Float32Array, tokens: typeof import('../tokens/tokens.js').tokens): Record<string, string>;
export const ORDER_W_TO_SLUG: Record<number, string>;
```

From canvas/src/rendering/saturation-map.ts:
```typescript
export const COLOR_SATURATION: Record<number, number>;
```

From canvas/src/tokens/tokens.ts:
```typescript
export const tokens: { colors: Record<TonalColorName, {...}>, orders: Record<OrderName, {...}>, axes: Record<AxisName, {...}> };
export const COLOR_W_TO_TONAL: Record<number, TonalColorName>;
```

From canvas/src/parser/parse-result.ts:
```typescript
export interface ParseResult {
  zip: string; numericZip: string;
  orderPos: number; axisPos: number; typePos: number; colorPos: number;
  weightVector: Float32Array; confidence: number;
  defaulted_dimensions: DimensionGroup[];
  matchedTerms: string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Weight vector spot-checks and keyword routing tests</name>
  <files>canvas/tests/weight-vectors.test.ts, canvas/tests/keyword-routing.test.ts</files>
  <behavior>
    - TEST-02: 10 zips produce SPECIFIC expected CSS values (not just shape checks):
      - 2123 (anchor): Strength Order tokens, Structured Color tokens, saturation ~0.50
      - 1213 (Foundation/Intense): Foundation density='airy', Intense saturation >=0.85
      - 3352 (Hypertrophy/Bodyweight): Hypertrophy tokens, bodyweight saturation ~0.40
      - 4145 (Performance/Mindful): Performance tokens, Mindful saturation ~0.10
      - 5241 (Full Body/Teaching): Full Body tokens, Teaching saturation ~0.05
      - 6313 (Balance/Intense): Balance tokens, Intense saturation >=0.85
      - 7218 (Restoration/Mindful): Restoration density='airy', Mindful saturation ~0.10
      - 2444 (Strength/Technical): Strength tokens, Technical saturation ~0.65
      - 3155 (Hypertrophy/Intense): Hypertrophy tokens, Intense saturation >=0.85
      - 6526 (Balance/Fun): Balance tokens, Fun saturation ~0.75
    - TEST-03: Known fitness terms route to correct dimensions:
      - "heavy barbell" -> orderPos=2 (Strength)
      - "restorative flow" -> orderPos=7 (Restoration)
      - "bodyweight pull-up" -> colorPos=2 (Bodyweight), typePos=2 (Pull)
      - "bench press" -> typePos=1 (Push)
      - "deadlift" -> typePos=2 (Pull)
      - "squat" -> typePos=3 (Legs)
      - "circuit training" -> colorPos=6 (Circuit)
      - Collision-prone: "leg press" -> typePos=3 (Legs), not typePos=1 (Push)
  </behavior>
  <action>
    Create `canvas/tests/weight-vectors.test.ts` with TEST-02 spot-checks. Follow the established pattern from rendering.test.ts: use `beforeAll` to resolve each zip once via `resolveZip()`, then assert specific CSS property values from `weightsToCSSVars()`. Each zip must have at least 3 assertions: (1) dominant Order CSS var matches expected, (2) dominant Color CSS var matches expected, (3) saturation value in expected range. Import resolveZip from ../src/weights/resolver.js, weightsToCSSVars from ../src/rendering/index.js, tokens from ../src/tokens/tokens.js.

    Create `canvas/tests/keyword-routing.test.ts` with TEST-03 keyword routing tests. Import scoreText from ../src/parser/scorer.js. Each test calls scoreText() with a known term and asserts the top result's orderPos, typePos, or colorPos matches the expected dimension. Include collision-prone terms (e.g., "leg press" must route to Legs not Push). Include at least 10 keyword routing assertions across all 4 dimensions (Order, Axis, Type, Color).

    Use the 10 zips from RESEARCH.md: 2123, 1213, 3352, 4145, 5241, 6313, 7218, 2444, 3155, 6526.

    NOTE: If any zip numeric positions do not match expected behavior when run, adjust assertions to match the ACTUAL pipeline output (the pipeline is already tested and correct from Phases 1-5). The test's job is to capture the known-good values as regression anchors.
  </action>
  <verify>
    <automated>cd canvas && npx vitest run tests/weight-vectors.test.ts tests/keyword-routing.test.ts</automated>
  </verify>
  <done>weight-vectors.test.ts has 10 zip spot-checks with specific CSS value assertions (30+ assertions total). keyword-routing.test.ts has 10+ keyword routing assertions covering all 4 dimensions including collision-prone terms. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full pipeline integration test</name>
  <files>canvas/tests/pipeline-integration.test.ts</files>
  <behavior>
    - TEST-05: Full pipeline chains without error:
      - "heavy barbell back work" -> ParseResult with orderPos=2, typePos=2 -> WeightVector -> 30+ CSS vars (all non-empty strings)
      - "bodyweight squats at home" -> ParseResult with colorPos=2 (Bodyweight) -> WeightVector -> CSS vars with low saturation
      - "tempo mindful stretching" -> ParseResult -> WeightVector -> CSS vars
      - "" (empty string) -> ParseResult with confidence=0, all 4 defaulted -> still produces valid CSS vars
    - Each stage output is the correct type (ParseResult[], Float32Array, Record<string, string>)
    - CSS vars contain no undefined or empty-string values
  </behavior>
  <action>
    Create `canvas/tests/pipeline-integration.test.ts` with TEST-05 integration tests. This file chains the complete pipeline: `scoreText()` -> take `results[0].weightVector` -> `weightsToCSSVars()`. Use 4 different text inputs as specified in behavior:

    1. Strength query: "heavy barbell back work" — assert orderPos=2, typePos=2, 30+ CSS vars
    2. Bodyweight query: "bodyweight squats at home" — assert colorPos=2 (Bodyweight)
    3. Ambiguous query: "tempo mindful stretching" — assert pipeline completes without error
    4. Empty string: "" — assert confidence=0, defaulted_dimensions has all 4 groups, pipeline still produces valid CSS vars from the defaulted zip

    Import scoreText from ../src/parser/scorer.js, weightsToCSSVars from ../src/rendering/index.js, tokens from ../src/tokens/tokens.js.

    For each test: (1) call scoreText(), (2) assert results array shape, (3) extract weightVector from top result, (4) call weightsToCSSVars(), (5) assert 30+ keys, all string values, no empty values.

    NOTE: For the empty string case, scoreText returns results with all dimensions defaulted — the pipeline must still produce valid CSS vars (defaults are valid zips). If scoreText returns an empty array for empty input, handle that case (skip the pipeline chain, assert the empty array).
  </action>
  <verify>
    <automated>cd canvas && npx vitest run tests/pipeline-integration.test.ts</automated>
  </verify>
  <done>pipeline-integration.test.ts has 4 integration test cases chaining scoreText() through weightsToCSSVars(). Each test asserts the correct output shape at every pipeline stage. All tests pass. Full Vitest suite (all 11 test files) passes: `cd canvas && npm test`.</done>
</task>

</tasks>

<verification>
Run the complete test suite to confirm no regressions:
```bash
cd canvas && npm test
```
All 11 test files (8 existing + 3 new) must pass. The 3 new files should add 40+ new test assertions.
</verification>

<success_criteria>
- 10 representative zips across all 7 Orders have specific CSS property value assertions (not just shape checks)
- Known fitness terms route to correct dimension positions
- Full pipeline chains from text input to CSS vars without error for 4 different inputs
- All existing 6,949+ tests continue to pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/08-tests-and-card-templates/08-01-SUMMARY.md`
</output>
